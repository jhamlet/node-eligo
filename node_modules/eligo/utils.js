
module.exports = require('protean').mixin({
    jsonValue: function (val) {
        var value;
        try {
            value = JSON.parse(val);
        }
        catch (e) {
            value = val;
        }
        return value;
    }
});
